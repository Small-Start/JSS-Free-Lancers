<%i=1
 		@post=Hash.new 
 		while i<=Post.count
 			p=Post.find(i)
 			if p.user_id==current_user.id%>
 				<h2><%=p.title%></h2>
 				<p><a href="<%=p.id%>">Manage this post</a>
 					<br/>
 			<%end
 			i+=1
 		end%>